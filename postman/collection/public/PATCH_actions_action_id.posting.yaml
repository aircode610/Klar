name: "PATCH /actions/{action_id}"
description: Frontend-facing root-level route. Send only the fields you want to change.
  Each changed field is logged to `UserCorrection` server-side.
method: PATCH
url: ${BASE_URL}/actions/{action_id}
body:
  content: |-
    {
      "status": null,
      "deadline": null,
      "title": null,
      "description": null
    }
