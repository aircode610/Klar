name: Consume a reset token and set a new password
description: Marks the token used, hashes the new password, and atomically deletes
  every existing session for that user.
method: POST
url: ${BASE_URL}/auth/reset-password
body:
  content: |-
    {
      "token": "",
      "new_password": ""
    }
