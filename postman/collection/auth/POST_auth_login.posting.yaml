name: "POST /auth/login"
description: Constant-time bcrypt verification — unknown email returns 401 just like
  wrong password (no user enumeration). On success the server sets the `klar_session`
  cookie.
method: POST
url: ${BASE_URL}/auth/login
body:
  content: |-
    {
      "email": "",
      "password": ""
    }
