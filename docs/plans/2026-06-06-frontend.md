# Frontend — As-Built Reference (Dev 1)

> **Status: BUILT.** This replaces the original 6-task plan (landing/login/signup/
> upload/SSE-results/dashboard). The frontend was built richer than the plan and
> then reconciled to the **implemented** backend, which pivoted to an
> obligation-centric model. This document describes **what actually exists** so a
> coding agent can run, understand, and extend it.
>
> - Product/screen spec: `docs/01-frontend.md`
> - Exact API contract (both directions, full JSON): `docs/06-frontend-integration-contract.md`

---

## What changed vs the original plan

| Original plan | As built | Why |
|---------------|----------|-----|
| Auth: `/login`, `/signup`, JWT in localStorage | **`/login` + `/signup`**, token in the store, `RequireAuth` gate, "continue as guest" | Frontend-driven; backend must add `/auth/*` (see `06`) for live |
| SSE streaming `/results/[id]` | **Synchronous** upload → letter detail | Backend `POST /letters` returns the full extraction in one response |
| `/dashboard` table | **`/deadlines`** iPhone-style calendar + agenda, **`/letters`** home, **`/documents`** | Obligation-centric UX |
| Response-letter draft, checklist, citations sections | **Obligations** (`actions[]`) + steps + evidence + RAG chat | Backend extracts actions, not drafts |
| Deep-blue palette, system fonts, CSS modules | **Editorial paper/ink + electric lime**, Clash Display / General Sans / Space Mono, **Tailwind v4** | Distinctive brand (see `01-frontend.md` §design) |
| `next-pwa` | **Serwist** (`@serwist/next`) | Modern SW tooling |
| `frontend/src/app/...` | App at **repo root** (`app/`, `components/`, `lib/`) | Single Next app at root |

> Note: `CLAUDE.md`'s repo-structure snippet still shows `frontend/src/app/…`; the
> actual app lives at the **repository root**. (Left as-is per scope: frontend-only.)

---

## Stack

- **Next.js 15** (App Router) · **React 19** · **TypeScript strict**
- **Tailwind CSS v4** (CSS-first `@theme`; design tokens as CSS variables)
- **motion** (`motion/react`) for animation
- **lucide-react** icons
- **zustand** for global state
- **MSW** for the mock API (mock mode)
- **Serwist** (`@serwist/next`) for the PWA service worker
- Fonts: Space Mono via `next/font`; Clash Display + General Sans via Fontshare CSS

Scripts: `npm run dev` · `npm run build` · `npm run start` · `npm run lint` ·
`npm run typecheck`.

---

## File structure (actual)

```
app/
  (app)/
    layout.tsx               # RequireAuth + sidebar (≥md) / bottom nav (<md) + top bar + Toaster
    letters/page.tsx         # Home: next-deadline banner, stats, grouped letter cards
    letters/[id]/page.tsx    # Letter detail (hero): summary, obligations, RAG chat
    deadlines/page.tsx       # iPhone-style calendar + agenda (from /actions)
    documents/page.tsx       # Searchable letter archive
    me/page.tsx              # Language, theme, profile vault, backend health, sign out
  login/page.tsx             # Sign in (AuthForm)
  signup/page.tsx            # Create account (AuthForm)
  (onboarding)/onboarding/page.tsx
  scan/layout.tsx            # RequireAuth gate for the scan flow
  scan/page.tsx              # Capture (camera / file / sample)
  scan/processing/page.tsx   # Reading animation → synchronous upload → routes to hero
  offline/page.tsx           # SW navigation fallback
  layout.tsx                 # root: fonts, theme/dir init script, grain overlay, Providers
  manifest.ts                # PWA manifest
  globals.css                # tokens, dark mode (data-theme), grain, fonts
components/
  ui/        Button, Card, Chip, DeadlineChip, BottomSheet, SegmentedControl,
             Toast, EmptyState, Screen, BottomNav, ThemeToggle, LangSwitcher, ScreenPlaceholder
  brand/     Wordmark, Stamp, HighlightText, OriginalLetter, ReadingLoader
  auth/      AuthForm, RequireAuth
  screens/   LetterCard, NextDeadlineBanner,
             calendar/Calendar, detail/ObligationCard, detail/LetterChat, me/ProfileVault
  app/       Sidebar, MobileTopBar
  Providers.tsx              # boots MSW (mock) or registers SW (live); syncs theme/dir
lib/
  api/client.ts              # typed client (appends ?lang to content endpoints)
  api/index.ts               # barrel
  api/mocks/                 # MSW: browser.ts, handlers.ts, fixtures.ts, content-i18n.ts
  adapt.ts                   # backend data → display (deadline urgency, category icon, severity)
  hooks.ts                   # useLetter, useActions, useLetters (re-fetch on lang change)
  store/index.ts             # zustand: auth session, lang, theme, onboarded, letters cache, pendingUpload
  urgency.ts                 # urgency colour map + countdown label
  calendar.ts                # date helpers for the calendar
  i18n/                      # dictionaries (en/de/fa) + dir map + getDictionary
  data/prototype.ts          # frontend-only example data (profile vault, calendar appts, quick Qs)
  utils.ts                   # cn(), formatMoney()
types/
  index.ts                   # the backend contract (Letter, ActionItem, …)
  extra.ts                   # frontend-only types (ProfileField, CalendarEvent, ChatMessage)
public/                      # grain.svg, icon.svg, icon-maskable.svg, mockServiceWorker.js
sw.ts                        # Serwist service worker source
```

---

## Data layer & backend integration

All network access goes through `lib/api/client.ts`. It reads
`NEXT_PUBLIC_API_URL` and talks to the backend's root routers (`/letters`,
`/actions`, `/rag`, `/health`) — **no `/api` prefix, no auth**. Content endpoints
carry the user's language as `?lang=`. The full request/response contract is in
**`docs/06-frontend-integration-contract.md`** — that is the source of truth.

- `lib/hooks.ts` — `useLetter(id)`, `useActions(status?)`, `useLetters()`. They
  depend on the current language, so switching language re-fetches localized data.
  `useLetters()` builds the home/documents feed from `/actions` + per-letter
  `GET /letters/{id}` (the backend has no list-letters endpoint) and falls back to
  the store's offline cache.
- `lib/adapt.ts` — maps backend data to display: `deadline` → urgency + countdown,
  `category` → icon + label, `severity` → label/colour, "handled" = all actions
  done/ignored.
- `lib/store` — persists the `auth` session, `lang`, `theme`, `onboarded`, and a **letters cache**
  (for the feed + offline). `pendingUpload` (a `File`) is staged by `/scan` and
  consumed by `/scan/processing` (not persisted).

### Mock mode

With `NEXT_PUBLIC_API_MODE=mock`, MSW (`lib/api/mocks/`) intercepts the exact same
requests and serves seven seeded letters + a mocked extraction on upload, and
**localizes content** by `?lang=` (German + Persian provided in
`content-i18n.ts`). This keeps the demo clickable and lets the backend be built in
parallel. Switch to `live` and point `NEXT_PUBLIC_API_URL` at the backend — no
code changes.

---

## Design system (summary)

Editorial, document-led, paper + ink with one electric-lime accent. Tokens live
as CSS variables in `app/globals.css` and are exposed through Tailwind (`bg-bg`,
`text-ink`, `bg-brand`, urgency colours…). Dark mode via `data-theme`. Four brand
devices: the **KLAR stamp** (`components/brand/Stamp`), the **highlighter sweep**
(`HighlightText`), **fog-to-clear** (`OriginalLetter`, `ReadingLoader`), and the
reimagined-officialdom document styling. Motion signatures are gated behind
`prefers-reduced-motion`. Full details in `docs/01-frontend.md`.

---

## Auth

Email+password login/signup (`components/auth/AuthForm`), with a "continue as
guest" option for the demo. The session (`{token,user}`) lives in the persisted
store; the API client sends it as `Authorization: Bearer` on every request.
`RequireAuth` gates the app shell and the scan flow, redirecting signed-out users
to `/login`. The backend endpoints this expects are specified in
`docs/06-frontend-integration-contract.md` §A (frontend-driven; not yet in the
implemented backend).

## Responsive & PWA

- **Breakpoint at `md` (768px):** **≥768px** (tablets + desktops) get the left
  sidebar rail + wide content; **<768px** (phones) get the fixed bottom tab bar (4
  tabs + center lime scan FAB) + slim top bar. The detail/calendar two-column
  content grids collapse below `lg` (1024px). Layouts verified from 320px up.
- **PWA:** `app/manifest.ts` (standalone, KLAR-stamp icons incl. maskable);
  `sw.ts` (Serwist) — network-first for `GET`s to `/letters`,`/actions`,`/rag`,
  `/health`; cache-first fonts; SWR images; `/offline` navigation fallback.
  Registered in production/live only (never shares scope with the MSW worker).

---

## i18n / RTL

`lib/i18n` holds per-language dictionaries (en/de/fa populated; tr/ar/uk fall back
to en) and a `dir` map that sets `rtl` for Persian/Arabic on `<html>`. UI uses CSS
logical properties throughout. **Letter content** is localized by the backend via
`?lang=` (emulated in mock); UI chrome is localized via the dictionaries.

---

## Environment

```
NEXT_PUBLIC_API_URL=http://localhost:8000   # backend origin (root routers), no /api
NEXT_PUBLIC_API_MODE=mock                    # mock | live
NEXT_PUBLIC_DEFAULT_LANG=en                  # en | de | fa | tr | ar | uk
```

---

## Verify

`npm run typecheck && npm run lint && npm run build` — all clean (13 routes).
Local: `npm run dev` → http://localhost:3000 (redirects to `/letters`).
