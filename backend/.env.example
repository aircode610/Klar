# =============================================================
# Klar backend — environment variables
# =============================================================
# Variable naming follows docs/02-backend.md spec. Legacy names
# (LLM_API_KEY, LLM_BASE_URL, CORS_ORIGINS) are still accepted
# as fallbacks for backwards compatibility.

# --- LLM provider (OpenAI-compatible API) ---
# WaveSpeed example:
#   QWEN_API_KEY=sk-ws-...
#   QWEN_API_BASE=https://api.wavespeed.ai/v1
# DashScope (Alibaba) example:
#   QWEN_API_BASE=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
QWEN_API_KEY=sk-ws-replace-me
QWEN_API_BASE=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen3.7-plus

# --- Persistence ---
DATABASE_URL=sqlite:///./data/klar.db
UPLOAD_DIR=./uploads

# --- ChromaDB (embedded, persistent) ---
CHROMA_PATH=./data/chroma
CHROMA_COLLECTION=klar_knowledge

# --- Auth ---
# Used to derive session token secrets — should be a 32+ byte random string.
JWT_SECRET=change-me-to-a-32-byte-random-string-in-prod
SESSION_TTL_HOURS=168
RESET_TOKEN_TTL_MINUTES=15
COOKIE_NAME=klar_session

# Cookie SameSite / Secure pairing — pick the recipe that matches your setup:
#
#   (A) Same-origin local dev (frontend + backend BOTH on localhost):
#       COOKIE_SECURE=false
#       COOKIE_SAMESITE=lax
#
#   (B) Cross-origin via ngrok / Vercel preview / production:
#       Frontend on localhost:3000 (or vercel.app), backend on *.ngrok.app — cookies
#       must travel cross-site. Browsers REJECT SameSite=None unless Secure=true.
#       COOKIE_SECURE=true
#       COOKIE_SAMESITE=none
#       (Set ALLOWED_ORIGINS below to include the exact frontend origin.)
#
# If you set SameSite=none without Secure=true, the backend will refuse to start
# (the cookie would never be stored by the browser).
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

# Dev/hackathon: expose reset tokens in /api/auth/forgot-password response
# so the frontend can test end-to-end without SMTP. SET TO false IN PROD.
DEV_AUTH_EXPOSE_RESET_TOKEN=true

# --- App ---
APP_ENV=dev
# Comma-separated list of allowed CORS origins. Include http://<lan-ip>:3000
# in addition to localhost when testing the PWA from a phone on the same
# WiFi (find your IP with `ipconfig getifaddr en0` on macOS). Also bind
# uvicorn to 0.0.0.0 so it accepts connections from the LAN, not just lo:
#   uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://192.0.0.2:3000,https://klar-app.vercel.app
