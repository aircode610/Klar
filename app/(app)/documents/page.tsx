import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export default function DocumentsPage() {
  return (
    <ScreenPlaceholder
      label="Documents"
      title="Documents"
      note="Every generated reply and form in one searchable place. Built in Phase 4."
    />
  );
}
