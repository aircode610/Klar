import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export default function DeadlinesPage() {
  return (
    <ScreenPlaceholder
      label="Deadlines"
      title="Deadlines"
      note="Upcoming deadlines, urgency-sorted, each linking back to its letter. Built in Phase 4."
    />
  );
}
