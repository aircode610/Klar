import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export default function MePage() {
  return (
    <ScreenPlaceholder
      label="Me"
      title="Me"
      note="Language, subscription, theme toggle, and privacy controls. Built in Phase 4."
    />
  );
}
